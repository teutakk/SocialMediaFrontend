import React from "react";
import classes from "./SingleNotification.module.css";

const SingleNotification = () => {
  return (
    <div className={classes["Single-Notification"]}>SingleNotification</div>
  );
};

export default SingleNotification;
